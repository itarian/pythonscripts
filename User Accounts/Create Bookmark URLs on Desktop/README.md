This script is used to create URL shortcuts on all user's desktop, 

       Note: Duplicate URLs will be auto removed

               You can able to change the URL shortcut for users by giving them on site list.

       Edit parameter:  sitelist

               sitelist = "www.google.com,www.gmail.com,www.youtube.com"

It should be run as a system user.