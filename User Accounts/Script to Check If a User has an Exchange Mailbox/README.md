The script checks if the user has an Exchange mailbox.
With the help of available default powershell multiple commands as scripts separating the users list.

Note: Run the script as system user only