Please refer the below script to turn OFF the capslock.

This script disables the capslock for single time only. If it is already enabled.

 

The script runs on both system user and login user.