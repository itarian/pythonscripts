This script is used to take a snap using webcam whenever system is loggedin and the image will be saved in specified location

For example:

webcam_logon_image2018_06_14_1624

In the Above mentioned example filename "webcam_logon_image" is default name followed by the timestamp (date(yyyy-mm-dd),time(hh:mm))

Run the script as system user